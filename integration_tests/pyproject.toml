[project]
name = "bokeh-integration-tests"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pytest>=8.0",
    "httpx>=0.27",
    "psycopg[binary]>=3.2",
    "pydantic>=2.12.5"
]

[tool.pytest.ini_options]
testpaths = ["."]
